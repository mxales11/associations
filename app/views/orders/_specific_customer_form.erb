<%= form_for(@order, url: new_customer_order_path(@customer)) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= "Customer name: "%>
  <br>
  <%= @customer.name %>
  </br>
  <div class="field">
    <%= f.label :item %><br />
    <%= f.collection_select :item_id, Item.all, :id, :name %>
  </div>
  
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :total_cost %><br />
    <%= f.number_field :total_cost %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>